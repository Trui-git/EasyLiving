<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.EasyLiving">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditCartActivity"></activity>
        <activity android:name=".CartHistoryActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".EditBillsActivity" />
        <activity android:name=".MedicineActivity" />
        <activity android:name=".TodoActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".BillsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>